// 'use client'

// import VisitorDetail from '@/components/dashboard/VisitorDetail'

// export default function VisitorDetailPage({
//   params,
// }: {
//   params: { id: string, hostelId: string }
// }) {
//   const { id, hostelId } = params;
//   return <VisitorDetail id={id} hostelId={hostelId} />
// }
