// Centralized icon exports to optimize bundle size
// This reduces the number of individual imports and improves tree shaking

// Common UI icons
export { UserIcon, PhoneIcon, MailIcon, MapPinIcon, BuildingIcon, HomeIcon, UsersIcon } from 'lucide-react';

// Action icons
export { PlusIcon, EditIcon, TrashIcon, XIcon, CheckIcon, ArrowLeftIcon, ArrowRight } from 'lucide-react';

// Status icons
export { AlertCircleIcon, ClockIcon, CheckCircleIcon, PencilIcon, FileTextIcon, LogOutIcon } from 'lucide-react';

// Navigation icons
export { MenuIcon, BellIcon, SearchIcon, FilterIcon, ListFilterIcon } from 'lucide-react';

// Feature icons
export { 
  Building2Icon, 
  GlobeIcon, 
  GraduationCapIcon, 
  TicketIcon, 
  ClipboardCheckIcon, 
  BarChartIcon,
  BedIcon,
  MessageSquareIcon,
  StarIcon,
  PlayIcon,
  RefreshCwIcon,
  UserCheckIcon,
  ClipboardIcon,
  UserPlusIcon
} from 'lucide-react';

// Clock and calendar icons
export { CalendarIcon } from 'lucide-react';

// PWA and connectivity icons  
export { 
  Wifi as WifiIcon, 
  AlertTriangle as ExclamationTriangleIcon,
  RefreshCw as ArrowPathIcon,
  Smartphone as DevicePhoneMobileIcon,
  Download as ArrowDownTrayIcon,
  Menu as Bars3Icon,
  X as XMarkIcon,
  Settings as CogIcon,
  Eye as EyeIcon,
  EyeOff as EyeSlashIcon,
  Lock as LockClosedIcon
} from 'lucide-react';
